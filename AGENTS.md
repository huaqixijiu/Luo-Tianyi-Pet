# 洛天依桌宠：Codex 开发约定

本仓库由 Codex 持续开发。任何新的 Codex 任务、压缩后的上下文或新的工作线程，都必须先按本文恢复项目状态，不能只依赖对话记忆。

## 每次开始工作

1. 阅读 `docs/项目总控.md`，确认当前阶段、阻塞项和唯一的“下一步”。
2. 按任务范围阅读对应的权威文档：
   - 产品行为：`docs/需求与场景设计.md`
   - 动画分配：`docs/动画状态映射.md`
   - 安全红线：`docs/安全与兼容性约束.md`
   - 技术结构：`docs/技术架构.md`
   - 里程碑：`docs/开发路线图.md`
   - 默认参数：`config/scene-rules.example.json`
3. 运行 `git status --short --branch`，保留并绕开用户已有修改。
4. 检查当前里程碑要求的环境和测试，不把“已写代码”误报成“已验证”。

## 文档职责

- `docs/项目总控.md` 是项目状态的唯一入口，只记录当前事实、当前风险和下一步。
- `docs/需求与场景设计.md` 记录用户已经确认的行为，不存开发进度。
- `docs/动画状态映射.md` 记录事件与动画的最终映射；建议项必须和确认项分开。
- `docs/安全与兼容性约束.md` 是硬约束，任何功能不得绕过。
- `docs/技术架构.md` 记录模块边界、数据流和工程结构。
- `docs/开发路线图.md` 记录里程碑及验收条件。
- `docs/决策记录.md` 追加架构或产品决策及理由，不悄悄改写历史。
- `docs/开发日志.md` 追加实际完成内容、验证结果、提交和下一步。
- `config/scene-rules.example.json` 是可执行默认规则的来源，已确认参数变更必须同步更新。

如果同一事实出现在多个地方，以职责最匹配的文档为准，并在同一变更中修复其他副本。用户最新的明确要求高于仓库旧文档；收到新决定后，应当立即写回对应文档和配置。

## 实现原则

- 目标平台是 Windows 10/11，使用 C#、.NET 10 LTS 和 WPF。
- 应用保持普通用户权限，不要求管理员权限。
- 原始素材保留在 `候选素材_官方/`，不移动、不重命名、不覆盖。
- 运行时素材放在 `assets/`；派生素材必须能够追溯到原文件和处理参数。
- 业务状态、动画调度和 Windows 平台能力分层，禁止把场景判断散落在窗口事件中。
- 用户显示模式、持续状态和一次性反应是三个独立状态维度。
- 外部应用联动只能使用 `docs/安全与兼容性约束.md` 允许的公开 Windows 能力。
- 文件拖放只能移入 Windows 回收站；失败时保留原文件，永不降级为永久删除。
- 不记录聊天正文、完整文件路径、窗口标题或用户输入。
- 优先做小而可验证的纵向功能，不一次引入大量未经验证的依赖。

## 质量门槛

每个可交付变更至少完成：

1. 编译成功；
2. 与改动风险相称的自动化测试；
3. 涉及窗口、透明度、命中或动画时进行实际视觉验证；
4. 涉及文件回收站、模拟输入、置顶或外部应用时进行安全降级测试；
5. 更新 `docs/项目总控.md` 和 `docs/开发日志.md`；
6. 形成一个说明清楚的 Git 提交；通过验证后推送到已配置远端。

如果本机缺少 SDK、权限或可复现条件，要把状态写成“阻塞”或“未验证”，不得用推测代替测试。

## 完成一轮工作后的交接格式

在 `docs/开发日志.md` 追加：

- 日期和目标；
- 修改的模块；
- 实际执行的测试及结果；
- 未验证内容或风险；
- Git 提交；
- 下一步的单一入口。

随后更新 `docs/项目总控.md` 中的当前里程碑、环境状态、已知问题和下一步，使下一次任务不需要翻阅全部聊天记录。
