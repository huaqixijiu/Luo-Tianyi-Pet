# 日历视觉设计实现 .81

2026-09-13，用户确认后实现并安装。没有新增运行依赖。

- 月历日期栏阳历／农历日／节日固定单行，班休小标记靠右；长节日省略、悬停完整。周历保留较完整信息。内容直接显示，无圆点折叠。
- 每周任意星期可选；周六日与仅周日是明确快捷项，日期例外优先。
- 单一内容编辑：旧Title/Notes只在显示时合并，新编辑存Content，标题摘要服务于列表。多日期选择开小窗，直接点击切换，确认才回写，取消保留原选择。
- 提醒展开后可本次不再提醒／隐藏倒计时，8秒撤销。跳过不删除行程，后续重复正常；隐藏仍到点。两种标记及内容已做实际保存重读验证。
- 卡片在原MediaControls可见边界下方，否则在桌宠下方；不足空间避让到上方，安全模式仍隐藏且不抢焦点。没有修改现有音乐岛外观或交互。

Core net10/net48各599项测试通过；应用双目标及主目录编译0警告／错误；net10/net48/发布版各26项WPF通过。实际截图见planner-ui-*.png；检查日志及安装证据见同名前缀txt/json。签名Valid、包Ok、102项安装文件哈希一致，正式进程响应。

限制：本轮未重测物理休眠、真实跨DPI多屏、长期性能。悬停由可见卡1秒刷新判断，自动验证采用点击展开；真实鼠标悬停时序待用户体验。UI按原生控件实现，并非生成图逐像素复刻。
