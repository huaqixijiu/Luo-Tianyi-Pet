# 日历全页面重做与行程分组管理（2026-09-13）

交付版本：0.1.0.82。白色／浅青圆角界面覆盖月历、周历、闹钟、行程编辑、多日期选择、班休和提醒。编辑改为居中面板，保留后方日历上下文；输入框、下拉框、复选框统一样式。月历节日不独占行，行程提醒图标固定在右侧。闹钟卡片时间不裁切，默认窗口760 DIP高且受工作区约束。底部入口切换日历、闹钟和行程管理。原音乐悬浮岛未修改。

多日期本来共用一条记录，现在明确按组展示：编辑可修改整组日期；删除支持仅这一天、整组和多选组，先展示影响范围。每天／每周等重复规则的单日删除使用ExcludedDates持久化。旧数据缺少字段时默认为空集合，不迁移或清空原数据。

验证：
- net10／net48编译均0警告0错误；Core测试各604项通过。
- 两目标及发布EXE的实际WPF检查各34项通过，覆盖真实按钮单日／整组／批量删除、重启持久化、保留无关闹钟、编辑器、六周月历、提醒安全降级和原音乐岛定位。
- 实际截图检查发现并修复月格滚动条、行程换行和倒计时时间截断。附图为运行截图，不是生成效果图。
- 签名Valid，安装状态Ok，102项安装文件哈希与布局一致，包内没有QA目录或测试数据。

限制：本轮未重新进行物理休眠、跨DPI多屏或长时间性能测量；悬停时序仍待真实使用反馈。密集行程可在日期格内滚轮查看，长内容悬停看全文或点击编辑。下一步唯一入口：本报告，收集实际UI反馈。
