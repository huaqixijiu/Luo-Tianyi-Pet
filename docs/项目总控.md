# 项目总控

> 最后更新：2026-08-30
>
> 当前阶段：M3 音乐和系统音量
>
> 项目状态：M2 已完成；M3 核心闭环已完成，剩余设置窗口音量滑块；并提前接入十二周年待机、身体互动和屏幕边缘躲藏
>
> 当前分支：`main`

本文是恢复项目上下文的第一入口。这里只保存当前状态；详细规则由下方权威文档维护。

## 1. 项目目标

制作一个仅在 Windows 本地运行的洛天依 Q 版桌宠，包含透明桌面窗口、两种待机显示模式、动画状态机、鼠标身体区域互动、网易云音乐快捷键控制、音乐与系统音量检测、QQ/微信来源提醒、原神低权限联动，以及把用户主动拖入的文件移入 Windows 回收站。

## 2. 已接管的默认技术决策

- C# + .NET 10 LTS + WPF；
- Windows 10 1809 及更高版本，重点验证 Windows 11；
- 普通用户权限运行，不以管理员身份启动；
- 首先提供自包含免安装构建，通知监听稳定后补充 MSIX 包；
- 状态机与 Windows 平台能力分层；
- GIF、动态 WebP、SVGA 和程序动效在构建阶段转换为统一运行时动画描述；
- 原始素材始终保留，派生资产写入 `assets/`；
- 配置采用带版本号的 JSON，并支持将旧配置迁移到新版本；
- 单实例运行，设置和位置按 Windows 用户保存在本地应用数据目录；
- 默认不开机自启，用户可在设置中主动开启。

这些默认决策由 Codex 负责维护。只有明显改变用户体验、隐私权限或安全边界的选择需要再次询问用户。

## 3. 当前真实状态

### 已完成

- 官方候选素材、动画挑选器和已选动画已进入仓库；
- 需求、动画映射、安全边界和示例配置已经形成；
- Git 仓库已绑定远端并使用 Git LFS 管理二进制素材；
- 已生成“享受音乐”0.5 倍速无缝运行时素材：移除每轮重复的文字入场，稳定动作帧往返闭环；
- 已生成带来源和哈希记录的“共鸣之声”倒放素材，用于降低音量；
- 已确认迷你“嘿嘿”和官方模型完整全身两种显示模式；
- 已确认音乐、拖拽、身体区域、时间问候、原神、消息和文件回收站的主要规则；
- 已建立跨对话恢复和项目接管规范。
- 已以用户级方式安装 .NET SDK 10.0.400，并写入用户 PATH 与 `DOTNET_ROOT`；
- 已创建 `LuoTianyiPet.sln`、4 个源码项目、3 个测试项目和资产编译工具；
- 已建立 Release 构建、全局编译规则、代码格式规则和 GitHub Actions CI；
- 已实现普通用户权限、单实例、透明无边框窗口、不进入任务栏和右键退出/置顶入口；
- 已实现本地设置的安全默认值、原子保存、损坏配置保留和最小隐私日志；
- 已建立可复现的透明 PNG 图集生成流程、带来源哈希的运行时动画清单和校验工具；
- 已用真实“嘿嘿”、完整全身待机、音乐摇摆、0.5 倍速享受音乐和裂开素材替换 M0 占位角色；
- 已实现毫秒级帧播放器、循环/单次结束回调、安全占位和两种显示模式状态解析；
- 已实现双击切换代码、音乐临时覆盖且不改变用户模式的状态模型，以及 M1 右键动画验证入口；
- 已实现用户菜单退出时的“裂开晃动”，并修复关闭窗口保存设置时可能出现的 UI 线程死锁；
- Release 编译达到 0 警告、0 错误，20 个自动化测试全部通过；
- 真实窗口验证了迷你 196×196、完整全身 256×276、音乐 206×206 的透明显示和连续帧变化；退出反应后进程正常以代码 0 结束。
- 完整全身动画连续运行 30.04 分钟：预热后的私有内存末 20 分钟波动仅 0.074 MB，稳定段约占单核 1.13%，没有持续增长；原始采样和分析保存在 `docs/validation/`；
- 已实现显示模式、持续状态和一次性反应三维状态机，包含优先级替换、同源合并、冷却、过期丢弃、令牌防止旧回调误结束新反应、拖拽打断及结束恢复；
- 现有音乐验证入口已接入状态机：每次进入播放状态随机选择“享受音乐”或“音乐摇摆”并在本次播放会话持续循环；暂停/停止后继续播放会重新随机；
- 音乐进入和停止使用青白光圈遮罩过渡；播放会话内不再在两套服装和画风之间切换，任意新动画仍可安全取消过渡；
- 已用独立手势判定器实现 300 ms 延迟单击、双击抑制和 6 像素拖拽阈值；拖拽形成后不会再触发身体单击；
- 迷你拖拽已接入“膨胀”，松手播放“降落”并叠加 420 ms 程序弹簧回弹；完整全身拖拽保持全身动画，松手不播放降落而直接恢复拖拽前持续状态；
- 已建立完整全身归一化身体区域和逐帧 alpha 命中；右键“动画与状态验证 → 显示身体区域调试层”可查看并校准眼睛、脸/嘴、手、胸部、下体、脚、头发和普通部位；
- 已将啾啾、心软软、可爱、击掌、重罪、黑化、糟了及 5 项普通部位随机池编译为统一运行时图集，动画目录扩展为 19 项；
- 已接入完整全身短按反应：眼睛心软软、脸/嘴啾啾、双手击掌回弹、脚部糟了晃动、普通部位 5 项随机且不连续重复；反应前后复用青白遮罩过渡并恢复用户原模式；
- 胸部和下体共享 4 秒重复窗口：首次重罪、窗口内再次点击黑化，黑化后共享 10 秒冷却；规则由独立 Core 解析器管理并可自动化测试；
- 已实现头发/头顶独立摸头手势：按住约 600 毫秒、累计 40 DIP 左右往返且至少两次换向后播放“可爱”；单向移出 30 DIP 或离开头部有效像素时转交窗口拖拽；
- 身体反应完成后进入 800 ms 恢复期，期间忽略身体单击和摸头，但双击切换模式及拖拽仍可用，避免动画尾部误触排队播放下一项；
- Release 编译 0 警告、0 错误，75 个自动化测试全部通过。
- 已进入 M3：平台层通过公开 Core Audio 会话枚举，只读取 `cloudmusic.exe` 对应会话的归一化峰值；不读取进程内存、模块、歌曲、窗口或网络数据；
- 已实现 250 毫秒轮询、0.001 默认有声阈值和 1.0 秒静音宽限；有声进入音乐状态，会话消失立即恢复，临时音频设备探测失败保持原状态并继续重试；
- 真实检测已接入现有状态机和随机音乐选择器：身体反应不会被强切，拖拽中只更新松手后的恢复状态，每次暂停后再次有声重新随机“享受音乐”或“音乐摇摆”；
- 网易云检测可在用户配置中独立关闭，旧配置自动补默认值；所有 `--preview-*` / `--qa-*` 运行关闭真实检测以保证验证可重复；
- 新增 `NAudio.Wasapi` 3.0.1 MIT 依赖并保存第三方许可说明；自动化测试覆盖有声、短静音、静音宽限、重新有声、会话消失、临时探测失败和目标进程不存在。
- Release 全量构建达到 0 警告、0 错误，92 个自动化测试全部通过；新版普通窗口在网易云未运行的真实环境中启用检测约 26 秒，没有误触发状态或错误，并以代码 0 正常退出。
- 已修正音乐状态拖拽外观：播放音乐时无论用户选择迷你还是完整全身模式，拖拽全程保持本次音乐动画，松手不播放降落；非音乐拖拽规则不变。
- 音乐连续静音宽限经实测后调整为 1.0 秒，在暂停反馈和切歌加载间隙之间折中；用户 `settings.json` 升级为版本 3，旧默认值会自动迁移，并保留窗口位置、置顶和媒体设置。
- 真实网易云日志已确认多次播放/停止转换，并在不同播放会话随机选中“享受音乐”和“音乐摇摆”；实测观察到约 0.5 秒切歌间隙，1.0 秒宽限可覆盖该类短间隙，超过 1 秒的网络加载仍需独立切歌状态处理。
- 已实现 Core `IMediaCommandSender` 与 Windows 用户级 `SendInput` 适配器；媒体命令包含上一首、播放/暂停、下一首和喜欢歌曲，默认组合为 `Ctrl+Alt+Left`、`Ctrl+Alt+P`、`Ctrl+Alt+Right`、`Ctrl+Alt+L`。
- 快捷键发送采用失败关闭：原神受保护进程前台或前台查询失败时拒绝，用户按住相关按键时拒绝，350 毫秒频率限制防止按键风暴，系统未完整接受输入时报告失败并尝试释放本次组合涉及的键。
- 已增加反馈气泡，明确区分“快捷键已发送，等待播放器响应”和已确认成功；安全模式、键盘占用、无效配置及系统拒绝显示具体原因，非限流失败播放“呜呜晃动”，尚未提前播放 OK。
- 正式媒体入口已从临时右键子菜单改为角色动画正下方的悬浮控制岛：采用 `#66CCFF` 天依蓝、蓝色玻璃表面和本地矢量图标，鼠标移入后上浮淡入，移出后短暂延迟淡出；窗口为控制区预留 58 DIP，控制岛不再覆盖角色、表情文字或身体命中区。播放状态中间键显示暂停图标，其他状态显示播放图标，工具提示展示当前快捷键。
- QA/预览运行使用独立单实例标识且不再保存用户设置；真实透明窗口已验证普通待机的隐藏/悬浮两态、音乐播放素材上的控制岛布局和播放/暂停图标切换。
- 候选素材库新增洛天依 12 周年 QQ 官方动态表情 16 个，覆盖出场、音乐、思考、消息提醒、休息、拒绝、生气、充电、情感反馈、纪念日和进食提醒等类别；挑选器总数增至 63，并提供“只看新添加”筛选。新增候选保持未分配状态，等待用户预览决定。
- 已接入当前曲目信息：优先读取 Windows 系统媒体会话；实测当前网易云未注册任何系统媒体会话，因此自动回退到只读的 `cloudmusic.exe` 主窗口标题。曲名与歌手显示在角色正上方，悬停时持续显示，切歌或外部标题变化后显示约 4 秒；不写日志、不保存播放历史。
- 歌名区域在窗口顶部独立预留 52 DIP，底部控制岛仍预留 58 DIP；真实网易云 QA 已读取并显示“奔向你 (Live)”与歌手“张睿”，人物及四键没有被覆盖。
- 用户已确认 `Ctrl+Alt+P` 的本机快捷键占用已经解决；仍保留键盘占用检测和受保护游戏失败关闭策略。
- “加载中摇摆”和“OK”已进入运行时图集；上一首/下一首发送后超过 400 ms 显示加载，曲名变化优先确认，Core Audio 静音后恢复辅助确认，成功播放 OK，15 秒无响应播放“呜呜晃动”并显示气泡。
- 十二周年素材按原 GIF 的首个完整动作周期编译，避免源文件内 13～20 次重复造成单次反应过长；已接入“躺平”5 分钟中等待机，以及 7 月 12 日、12 月 12 日待机中每 45～90 分钟随机一次的“生日快乐”彩蛋。
- 完整全身命中已拆分脸部与嘴部，并缩小胸部、下体矩形；嘴部仍为“啾啾”，脸部改为“贴贴”，普通身体改为“抱抱”，眼睛、手、脚和敏感区域规则不变。
- 已实现左右及底部屏幕边缘躲藏：左右用“偷看”，左侧水平镜像；底部用“闪亮登场”。躲藏倒放并保留 12 DIP 触发条，悬停正放一次定格，移出倒放。
- 新增两项官方摸头候选“幻游世界·摸摸”和“夜航星·摸摸”，已标记“新添加”进入动画挑选器；当前运行时仍保留“可爱”，等待用户预览决定。
- 已修复“享受音乐”持续播放的循环接缝：运行时不再从原 GIF 第 9 帧跳回文字为空的第 0 帧，而是只在文字稳定的第 4～9 帧之间按相邻帧往返，文字不会反复出现，0.5 倍速和 2 秒周期保持不变。
- 已接入 Windows 默认多媒体输出端点的系统主音量：指针位于桌宠窗口时滚轮每格默认调整 2%，受保护游戏前台或前台查询失败时不写入；不使用全局鼠标 Hook 或键盘模拟。
- 已接入外部系统音量和静音变化反馈：Core Audio 通知配合 250 ms 只读采样兜底；提高播放原版“共鸣之声”，降低播放倒放版，静音/解除静音只更新图标与百分比。
- 连续同方向音量变化共享一个状态机反应并实时更新气泡，停止 500 ms 后恢复此前持续状态，同方向动画冷却 2 秒；通知与轮询重复值不会二次触发。

### 尚未完成

- 用户实际双击/拖动手感、身体区域边界微调、DPI、多显示器和全屏行为仍待后续互动窗口统一验收；
- QQ/微信通知监听尚未做权限和兼容性原型；
- 网易云快捷键、切歌确认和系统音量核心闭环已经完成；设置窗口音量滑块与回收站操作尚未完成。

## 4. 已确认但仍需实现时细化的规则

- 音乐从暂停或停止进入播放时，随机选择“享受音乐”或“音乐摇摆”并持续循环；音乐停止后恢复用户选择的“嘿嘿”或完整全身模式。
- 音乐状态临时覆盖显示模式，但不修改用户的模式选择。
- 暂停动画期间恢复播放时立即中断暂停反馈，并重新随机选择一个音乐持续动画。
- 用户从菜单退出桌宠时播放一次“裂开晃动”；系统关机、注销或崩溃路径不等待该动画。
- 完整全身身体区域只在完整全身待机可见且没有一次性反应覆盖时接受点击。
- 一次性反应过期后不补播；同源通知短时间合并。
- 原神等受保护进程位于前台时，停用模拟输入、文件接收和普通临时置顶。
- 普通全屏应用前台时桌宠退到桌面层；受保护游戏独占全屏时可隐藏并降低帧率。
- 调高音量播放原版“共鸣之声”，调低音量播放倒放版；静音和解除静音暂不播放动画。
- 操作失败或超时统一播放“呜呜晃动”，同时显示与失败原因对应的气泡文字。
- 完整全身模式点击眼睛播放“心软软”，点击脚播放“糟了晃动”。
- 完整全身模式点击普通身体部位固定播放“十二周年·抱抱”；脸部播放“贴贴”，嘴部播放“啾啾”。“可爱”暂时继续用于摸头，等待两项官方“摸摸”候选的最终选择。

## 5. 暂不阻塞开发的待定动画

- 通用警告动画的最终选择；
- 随机待机动画池、权重与冷却；
- 强节拍舞蹈是否默认启用；
- 睡眠苏醒动画。

这些位置先使用可配置占位或无动画安全降级，不阻塞工程骨架。

## 6. 当前风险

1. 透明窗口已移除阻塞式 `DragMove` 并改为捕获鼠标后的手动移动；当前按虚拟桌面边界夹取，但跨 DPI 显示器仍需实测。
2. QQ/微信提醒依赖用户授予 Windows 通知访问权限；来源应用关闭系统通知时不能保证检测。
3. 透明异形窗口的像素命中、拖放扩展区域和点击穿透需要实际 Windows 验证。
4. 全身模型和表情包画风不同；音乐和身体短按反应已经复用遮罩式过渡，后续新素材仍需逐项检查缩放和遮罩覆盖范围。
5. 文件回收站功能具有数据操作风险，只能用临时文件测试，且失败不能永久删除。
6. 当前网易云快捷键使用首轮默认组合；若用户在网易云中改过全局快捷键，需要在后续设置页同步修改，否则桌宠只能确认“输入已发送”，不能保证播放器响应。
7. 当前网易云版本没有注册 Windows 系统媒体会话，曲名依赖其主窗口标题兜底；如果网易云以后隐藏标题、改格式或启用不带标题的迷你模式，桌宠只能安全降级为“暂未读取到歌曲名称”。
8. 系统音量服务当前绑定启动时的默认多媒体输出设备；运行期间切换默认声卡会安全降级为暂不可用，自动重建端点留到设置/设备切换完善项。

## 7. 权威文档索引

| 内容 | 文档 |
|---|---|
| Codex 工作方式 | `AGENTS.md` |
| 用户确认的行为 | `docs/需求与场景设计.md` |
| 动画与场景映射 | `docs/动画状态映射.md` |
| 安全红线 | `docs/安全与兼容性约束.md` |
| 技术模块和数据流 | `docs/技术架构.md` |
| 里程碑和验收条件 | `docs/开发路线图.md` |
| 决策及理由 | `docs/决策记录.md` |
| 实际开发与测试历史 | `docs/开发日志.md` |
| 默认可执行参数 | `config/scene-rules.example.json` |

## 8. 下一步

唯一的下一步是建立设置窗口的第一条纵向链路：提供系统音量滑块、滚轮控制开关、外部音量反馈开关和每格步长，并复用现有 `ISystemVolumeService`，不把设置塞回右键快捷菜单。
